var for_graph = function(value) {
  if (value === 5) {
    return 100
  } else if (value === 4) {
    return 80
  } else if (value === 3) {
    return 60
  } else if (value === 2) {
    return 40
  } else {
    return 20
  }
}

console.log('Create works');

for_graph(<%=  %>)
// $('reviews')
